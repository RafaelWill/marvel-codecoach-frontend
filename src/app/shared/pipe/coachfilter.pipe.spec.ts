import { CoachfilterPipe } from './coachfilter.pipe';

describe('CoachfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CoachfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
